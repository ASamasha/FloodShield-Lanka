name: floodshield_lanka
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  shared_preferences: ^2.2.2
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  fl_chart: ^1.2.0
  intl: any
  cupertino_icons: ^1.0.8
  geolocator: ^13.0.1
  battery_plus: ^6.1.1
  url_launcher: ^6.3.0
  video_player: ^2.14.0
  audioplayers: any
  syncfusion_flutter_pdfviewer: any
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
